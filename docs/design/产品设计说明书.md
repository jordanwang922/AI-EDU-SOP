# 产品设计说明书（AI 亲子教育 SOP 系统）

> 版本：0.4  
> 说明：本文档在 PRD《完整问题树 + H5 + 后台》基础上，结合当前阶段目标补充**非静态、后台驱动、AI 辅助生产、多端演进**等产品决策，随迭代更新版本号与修订记录。

## 1. 产品愿景

家长在孩子教育场景中遇到的具体问题时，能在移动端**快速定位问题 → 获得可执行的 SOP 与可复制提示词 → 真实改善亲子互动与学习结果**；运营侧可在后台持续维护分类与词条，并借助 AI **批量生成初稿、人工审核后入库发布**。

长期目标：用户持续使用线上系统，形成**用户资料、行为与反馈数据**的沉淀，支撑个性化建议与后续增值服务（含独立售卖可能）。

## 2. 目标用户与场景

- **家长（主用户）**：小学生至高中阶段孩子的家长，需在碎片时间用手机解决问题。
- **运营 / 教研 / 编辑（后台用户）**：维护分类树、词条、提示词包、上下架与审核；部分内容由 **AI 辅助生成** 后人工修订。
- **训练营学员**：课程与线上系统打包售卖；系统既是交付物，也是后续留存与转化的载体。

## 3. 1.0 产品边界（共识）

以下内容已与架构 / 数据预留策略对齐（详见 `docs/architecture/数据模型与扩展性.md`）：

| 维度 | 1.0 约定 |
| --- | --- |
| **家长端登录 / 付费** | **不做**。公开发布的 H5 直接浏览树状内容与词条详情（后续再接账号与订单）。 |
| **家长端 AI** | **不做**对话式生成、不改写提示词等实时 AI；家长只看到**已入库、已发布**的固定内容与提示词。 |
| **后台 AI** | **必须做**：以「输入题目 / 主题 → **生成整套结构化词条模板**」为主流程；绝大多数内容由 AI 起草，**生成者与审核者可同为一人**（编辑自检后发布）。 |
| **数据库** | 虽无终端登录，仍预留 **用户 / 订单 / 权益** 等扩展表或字段，避免日后付费接入时大迁移。 |

## 4. 核心体验（家长端）

1. **场景入口**：从「我现在遇到什么问题」进入；**逐层下钻已收束为三层路径**（一级大类 → 该大类下词条列表 → 词条详情），**取消单独的问题列表层**，以减少点击；详见 **`docs/architecture/系统设计规则.md`**。辅助关键词 / 标签搜索；远期再强化「一句自然语言直接问」。
2. **叶子页统一结构**：问题本质、误区、原则、SOP、AI 提示词、示例、话术、任务、观察指标等（见 PRD 模板）；用户进入详情后应一次获得完整解决方案与可复制提示词。
3. **执行辅助**：一键复制提示词、阅读操作步骤；后续迭代可增加生成话术、打卡、收藏等（见 PRD 路线图）。
4. **排序**：一级大类与词条列表支持 **运营手工顺序 + 点击热度自动上浮**（点击越多越靠前），冷启动以手工顺序为准；规则见 **`docs/architecture/系统设计规则.md` §2**。

## 5. 产品形态演进：速查手册 → 语义问答

本节与 PRD 中的 V1 / V2 能力对应，把**商业上可交付的 1.0**和**远期能力**写清，避免工程上走偏（例如 1.0 不必上完整 RAG，但数据与 API 要预留扩展）。

### 5.1 1.0 / 近期：树状 FAQ +「AI 教育速查手册」

- **内容规模**：先以约 **100～200 个高质量叶子问题**为首批（可对应 PRD 的 MVP 清单再扩展），在后台按**多级分类树**分门别类、审核发布（后台分类粒度可比前台导航更细）。
- **主要动线**：用户**不依赖**自然语言智能问答，而是 **一级大类 → 该大类下词条列表 → 词条详情**（三层）；PRD 中的「二级主题页 + 问题列表页」不再作为必选路由层，详见 **`docs/architecture/系统设计规则.md`**。
- **页内价值**：阅读 SOP、复制 **已预置、经审核的提示词**、按页内指导操作；定位是**可执行的速查与行动指南**，不是开放域聊天机器人。
- **搜索（可选增强）**：关键词 / 标签搜索作为补充，降低「只有点树才能找到」的摩擦；仍属 1.0 范围。

### 5.2 远期：自然语言提问 + 基于库的回答与生成

- **用户行为**：用户用**口语化、语义化**的问题提问，未必匹配目录文案，也未必愿意逐层点击。
- **系统行为**：**基于内容库**（已发布的词条、提示词、话术与结构化字段）做检索与综合，由 **AI 组织答案**；并可**动态生成或改写提示词**（仍建议保留「可复制」与合规边界）。
- **与 1.0 的关系**：1.0 沉淀的**结构化库**是远期的**唯一权威来源**；远期能力是在库之上的**检索 / 排序 / 生成层**，而不是另起一套未经审核的闲聊。

### 5.3 产品表述（对内对齐）

- **1.0**：「**三层导航** + 固定词条 + 一键复制提示词的 AI 教育速查手册；列表顺序随点击热度优化。」  
- **远期**：「以官方知识库为准的语义问答与个性化提示词输出（具体形态见架构文档中的能力分期）。」

## 6. 后台与内容生产（AI 为主）

系统必须支持**完整的内容发布闭环**，而不是静态页面手工改 HTML：

- **分类与词条管理**：多级分类、排序、可见性、年龄段、状态（草稿 / 待审 / 已发布）、版本。
- **后台 AI 生成（1.0 必备）**  
  - 典型交互：**录入一个问题标题或选题** → 调用大模型 **一次性生成符合 PRD 模板的完整词条**（含 SOP、提示词、话术等字段）。  
  - 绝大多数内容预期 **100% 由 AI 起草**；运营可在表单内微调后再发布。  
  - **审核责任**：当前约定 **生成者与审核者为同一角色即可**（自检后发），系统仍保留「草稿 / 待发布 / 已发布」状态与可选双人复核扩展。
  - **禁止**：未发布草稿不得出现在家长端 API。
- **可选**：语音录入、多模型路由等可在后续迭代接入（与火山方舟 / 豆包等密钥沿用既有项目实践）。

人工把控教育质量与合规的责任仍在运营侧；产品上保留后续「双人审核」扩展空间。

## 7. 商业与打包形态

- **训练营独立售卖**：课程 + 本线上系统作为打包交付；账号与权益策略在 implementation 阶段细化（授权周期、是否绑定订单等）。
- **系统可能单独收费**：产品层保留「订阅 / 按年 / 与课程拆分售卖」等可能性；计费与支付接入在架构与后续迭代中预留扩展点。
- **1.0**：交付形态可先以「无需登录的速查 H5」验证内容与训练营闭环；**付费与账号体系从架构与数据层预留**，产品上不必在 1.0 全部兑现。

## 8. 数据与反馈（留存目标）

前期即应考虑（不必第一期全部实现）：

- **用户侧**：基础画像、孩子学段、收藏、搜索词、浏览与复制行为（需隐私政策与用户同意）。
- **反馈**：有用 / 没用、不懂、希望补充的问题等（见 PRD 后台模块）。
- **目的**：优化内容排序与选题、支撑后续个性化与运营触达；与「孩子学习变好、家长能力提升」的长期叙事一致。**远期语义问答可反哺「搜不到的词」与选题迭代。**

## 9. 客户端演进路径

| 阶段 | 客户端 | 产品备注 |
| --- | --- | --- |
| 前期 | **H5** | 快速触达、嵌入训练营交付链路 |
| 中期 | **小程序**（微信等） | 便于分享、登录与消息触达；需与同一后端 API 对接 |
| 远期 | **原生 App**（可选） | 更强推送与端能力；仍应共享核心业务 API |

设计原则：**业务能力集中在服务端与统一 API**，前端多渠道薄封装，避免把业务规则写死在某一个 H5 页面里。

## 10. 与 PRD 的关系

详细问题树、字段全集、后台模块清单与版本规划（V1 / V1.5 / V2 / V3）以 `docs/prd/AI亲子教育SOP系统_完整问题树_H5后台设计_V1.1.md` 为准。**家长端浏览层级与排序规则**以 **`docs/architecture/系统设计规则.md`** 为准（PRD 中「四层页面层级」为历史叙述，前台路径已收束）。本文档负责收敛**当前共识级**的产品边界与演进方向；其余冲突以**更新日期更新的文档**或**显式评审结论**为准。

## 11. 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 0.4 | 2026-05-11 | 对齐 **`系统设计规则`**：三层浏览（取消中间问题列表层）、一级与词条热度排序；更新与 PRD 层级差异说明 |
| 0.3 | 2026-05-10 | 新增「1.0 产品边界」：无登录无付费、家长端无 AI、后台必选 AI 生成；生成与审核可为同人；数据预留说明关联架构文档 |
| 0.2 | 2026-05-10 | 新增「产品形态演进」：1.0 树状速查手册（约 100～200 题）、远期语义问答与基于库的生成 |
| 0.1 | 2026-05-10 | 初稿：非静态、AI 辅助入库、训练营打包、数据留存、多端演进 |
